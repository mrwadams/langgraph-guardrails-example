# Core dependencies
langgraph>=0.0.20
langchain>=0.1.0
langchain-core>=0.1.0
python-dotenv>=1.0.0

# LLM providers (Anthropic for model-based guardrails)
langchain-anthropic>=0.1.0

# Optional: OpenAI (alternative provider for LLM and embeddings)
# langchain-openai>=0.0.5
# openai>=1.0.0

# Embedding-based guardrails
numpy>=1.24.0
sentence-transformers>=2.2.0  # Local embeddings (no API required)

# Optional: Advanced PII detection
# presidio-analyzer>=2.2.0
# presidio-anonymizer>=2.2.0

# Optional: Toxicity detection
# detoxify>=0.5.0

# Optional: Language detection
# langdetect>=1.0.9

# Development dependencies
pytest>=7.0.0
pytest-asyncio>=0.21.0
black>=23.0.0
