# Core dependencies
langgraph>=1.0.0
langchain>=1.0.0
langchain-core>=1.0.0
python-dotenv>=1.0.0

# LLM providers (Anthropic for model-based guardrails)
langchain-anthropic>=1.0.0

# Optional: OpenAI (alternative provider for LLM and embeddings)
# langchain-openai>=0.3.0
# openai>=1.0.0

# Embedding-based guardrails
numpy>=2.0.0
sentence-transformers>=5.0.0  # Local embeddings (no API required)

# Optional: Advanced PII detection
# presidio-analyzer>=2.2.0
# presidio-anonymizer>=2.2.0

# Optional: Toxicity detection
# detoxify>=0.5.0

# Optional: Language detection
# langdetect>=1.0.9

# Optional: Visualization (for ASCII diagram generation)
# grandalf>=0.8

# Development dependencies
pytest>=8.0.0
pytest-asyncio>=0.23.0
black>=24.0.0
